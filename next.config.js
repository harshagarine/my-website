/** @type {import('next').NextConfig} */
const nextConfig = {  
    basePath: '/my-website', // Replace 'repo-name' with the actual name of your GitHub repository
    assetPrefix: '/my-website/tree/main/public', // This ensures the correct path for static assets}
}

module.exports = nextConfig
